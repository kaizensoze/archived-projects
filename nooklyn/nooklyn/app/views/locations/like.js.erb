$('<%= "#location_card_#{@location.id}" %> .like-notice').fadeIn().delay(1000 * 2).fadeOut();
$('<%= "#location_card_#{@location.id}" %> .button_to').replaceWith('<%=j render 'unlike_button', :location => @location %>');
