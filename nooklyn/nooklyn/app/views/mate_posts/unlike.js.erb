$('<%= "#mate_card_#{@mate_post.id}" %> .unlike-notice').fadeIn().delay(1000 * 2).fadeOut();
$('<%= "#mate_card_#{@mate_post.id}" %> .button_to').replaceWith('<%=j render 'like_button', :mate_post => @mate_post %>');
