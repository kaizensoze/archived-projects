<%= form_for @guide_story_photo, :html => { :multipart => true } do |f| %>
  <% if @guide_story_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guide_story_photo.errors.count, "error") %> prohibited this photo from being saved:</h2>
      <ul>
      <% @guide_story_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="row">
      <div class="col-xs-6">
        <%= f.label :image, "File", class: "general-text-label" %><br>
        <%= f.file_field :image %>
      </div>
      <div class="col-xs-6">
        <%= f.label :caption, "caption", class: "general-text-label" %><br>
        <%= f.text_field :caption, class: "general-text-field" %>
      </div>
    </div>
  </div>
    <hr>
  <div class="actions">
    <%= f.submit 'Submit', class: "button btn-blue btn-2x" %>
  </div>
<% end %>
