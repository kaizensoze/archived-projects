$('<%= "#room_card_#{@room_post.id}" %> .unlike-notice').fadeIn().delay(1000 * 2).fadeOut();
$('<%= "#room_card_#{@room_post.id}" %> .button_to').replaceWith('<%=j render 'like_button', :room_post => @room_post %>');
