<div>
  <%= form_for [:cms, @did_you_know_subject, @did_you_know_item], html: {class: "form-horizontal", role: "form"} do |f| %>
    <div class="action-buttons top">
      <%= render 'form_action' %>
    </div>
    
    <% if @did_you_know_item.errors.full_messages.any? %>
      <div class="alert alert-danger alert-dismissible" role="alert">
        <% @did_you_know_item.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>

    <div class="form-group">
      <label class="col-sm-2 control-label">Title</label>
      <div class="col-sm-10">
        <%= f.text_field :title, class: "form-control" %>
      </div>
    </div>

    <div class="form-group">
      <label class="col-sm-2 control-label">Website</label>
      <div class="col-sm-10">
        <%= f.text_field :website, class: "form-control" %>
      </div>
    </div>

    <div class="form-group">
      <label class="col-sm-2 control-label">Email</label>
      <div class="col-sm-10">
        <%= f.text_field :email, class: "form-control" %>
      </div>
    </div>

    <div class="form-group">
      <label class="col-sm-2 control-label">Phone Number</label>
      <div class="col-sm-10">
        <%= f.text_field :phone_number, class: "form-control" %>
      </div>
    </div>

  <% end %>
</div>

<%= form_for [:cms, @did_you_know_subject, @did_you_know_item], method: :delete, html: {id: "delete_did_you_know_subject_did_you_know_item"} do |f| %>
<% end %>
